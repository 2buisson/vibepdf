# File list CommandBar

Add a `CommandBar` at the top of the file list with the following primary `AppBarButton` (label bellow the icon):

- Add PDF(s) with add icon -> replaces the bottom right button
- Remove with trash icon -> replaces the delete icon button currently located above the preview section